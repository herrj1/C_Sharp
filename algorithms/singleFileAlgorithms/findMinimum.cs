//Find mininum. Function only. No driver is implemented.
static int findMinimum(int[] arr){
	int min = arr[0];
	for(int i = 0; i<arr.Length-1; i++){
		if(arr[index] < min){
			min = arr[index];
		}
	}
	return min;
}