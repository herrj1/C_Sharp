//find maximum. Function only. No driver implemented here.
static int findMaximum(int[] arr){
	int max = arr[0];
	for(int i = 0; i < arr.Length-1; i++){
		if(arr[index] > max){
			max = arr[index];
		}
		return max;
	}
}